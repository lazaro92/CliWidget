#ifndef MULTI_SELECT 
#define MULTI_SELECT

#include <vector>
#include <string>

namespace CliWidget {

    class MultiSelect: public Widget {
       public:
           Widget() = default;
           Widget(const)
    }
}

#endif
